import { commonProperties } from './commonProperties';

export default [
  commonProperties.context,
  commonProperties.type('AudioObject'),
  commonProperties.id,
  // TODO: Add audio object specific fields
];
