import { commonProperties } from './commonProperties';

export default [
  commonProperties.context,
  commonProperties.type('SearchAction'),
  commonProperties.id,
  // TODO: Add search action specific fields
];
