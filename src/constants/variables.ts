/**
 * Template Variables
 *
 * Defines available template variables that can be used in schema fields.
 * These variables are replaced with actual values at runtime.
 *
 * Currently empty as this is a standalone app without WordPress integration.
 * Users can implement their own variable system as needed.
 */

export const variables = []
